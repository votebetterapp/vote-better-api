require 'rails_helper'

RSpec.describe School, type: :model do
  it "must have a name and email_signature" do
    skip('implement this after stephanie approves this test case')
  end

  it "email signature is inferred by the god user who creates it" do
    skip('implement this after stephanie approves this test case')
  end
end
